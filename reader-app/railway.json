{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:railway && npx tsx prisma/seed.ts"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && cd .next/standalone && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
