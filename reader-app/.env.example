# Reader App — copy to .env and adjust as needed

# PostgreSQL connection (matches docker-compose defaults)
DATABASE_URL="postgresql://reader:reader@localhost:5434/reader?schema=public"

# Host port for PostgreSQL (change if 5434 is taken)
POSTGRES_PORT=5434
