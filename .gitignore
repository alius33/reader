# Extracted text
extracted/

# Obsidian workspace (user-specific)
summaries/.obsidian/workspace.json

# Temp files
temp_extract*.txt

# Reader app
reader-app/node_modules/
reader-app/.next/
reader-app/.env
reader-app/tsconfig.tsbuildinfo
reader-app/public/sw.js
reader-app/scripts/.cover-backfill-results.jsonl

# Inbox binary source files (too large for git)
inbox/*.pdf
inbox/*.epub
inbox/*.mobi
inbox/*.azw3

# Working folders (source material, not needed in deployment)
test 496/
